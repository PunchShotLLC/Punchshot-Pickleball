import { makeStyles }  from '@material-ui/core/styles';

const useStyles = makeStyles({
    
});

export const Home = () => {
    const classes = useStyles();

    return (
        <div>
            
        </div>
    )
}